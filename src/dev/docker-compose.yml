version: "3"

services:
   mysql:
     image: mysql:5.7
     restart: always
     container_name: training-day-mysql
     environment:
       MYSQL_PASSWORD: password
       MYSQL_ROOT_PASSWORD: password
       MYSQL_DATABASE: nest
     ports:
       - "3307:3306"